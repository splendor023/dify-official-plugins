[project]
name = "dify-arxiv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "arxiv>=4.0.0",
    "dify_plugin>=0.9.0",
    "pydantic>=2.13.4",
]

# uv run black . -C -l 100 && uv run ruff check --fix
[dependency-groups]
dev = []
