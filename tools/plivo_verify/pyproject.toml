[project]
name = "dify-plivo-verify"
version = "0.1.0"
description = "Plivo Verify OTP tool plugin for Dify"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.9.0",
    "plivo>=4.60.1",
]

[dependency-groups]
dev = []
