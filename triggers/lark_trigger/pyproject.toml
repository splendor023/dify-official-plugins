[project]
name = "lark_trigger"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"

# uv lock
dependencies = [
    "lark-oapi>=1.6.9",
    "cachetools>=7.1.4",
    "dify_plugin>=0.9.1",
    "werkzeug>=3.1.8",
]

# uv run black . -C -l 100 && uv run ruff check --fix
[dependency-groups]
dev = []
